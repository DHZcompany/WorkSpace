<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"   
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zwj.dao.UserDao">
	<select id="getUserById" parameterType="string" resultType="com.zwj.entity.User">
		SELECT * FROM user_t WHERE id=#{id}
	</select>
	<select id="getById" parameterType="string" resultType="com.zwj.entity.User">
		SELECT * FROM user_t WHERE id=#{id}
	</select>
	<select id="getAll"  resultType="com.zwj.entity.User">
		SELECT * FROM user_t
	</select>
		<select id="getAllUser"  resultType="com.zwj.entity.User">
		SELECT * FROM user_t
	</select>
	
</mapper>